[project]
name = "mindovermatter"
authors = ["JackScott7"]
version = "0.1.0"
description = " A Feature-rich Todo CLI, while being local, backed by SQLite"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rich>=14.3.2",
    "typer>=0.21.1",
]
